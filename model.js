let Model = {

  /**
   * Takes a string and sets the title to the input string.
   */
  initialize: (title) => {
    this.title = title;
  },

  /**
   * Returns title.
   */
  getTitle: () => this.title,

};